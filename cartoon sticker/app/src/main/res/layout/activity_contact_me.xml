<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".contact_me">

    <Button
        android:id="@+id/btn_to_main2"
        android:layout_width="match_parent"
        android:layout_height="62dp"
        android:layout_margin="5dp"
        android:layout_weight="1"
        android:backgroundTint="@color/orangedark"
        android:fontFamily="@font/audiowide"
        android:text="Back"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btn_sendmail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:layout_weight="1"
        android:backgroundTint="@color/orangedark"
        android:fontFamily="@font/audiowide"
        android:text="send mail"
        app:layout_constraintBottom_toTopOf="@+id/btn_to_main2"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <LinearLayout
        android:id="@+id/linearLayout2"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toTopOf="@+id/btn_sendmail"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0">

        <EditText
            android:id="@+id/name"
            android:layout_width="match_parent"
            android:layout_height="41dp"
            android:layout_margin="10dp"
            android:background="@color/orange"
            android:ems="10"
            android:hint="your name..."
            android:inputType="textPersonName"
            android:textColor="@color/orangedark"
            android:textColorHint="@color/orangetexthint" />

        <EditText
            android:id="@+id/subject"
            android:layout_width="match_parent"
            android:layout_height="41dp"
            android:layout_margin="10dp"
            android:background="@color/orange"
            android:ems="10"
            android:hint="Subject...."
            android:inputType="textPersonName"
            android:textColor="@color/orangedark"
            android:textColorHint="@color/orangetexthint" />

        <MultiAutoCompleteTextView
            android:id="@+id/body"
            android:layout_width="match_parent"
            android:layout_height="192dp"
            android:layout_margin="10dp"
            android:background="@color/orange"
            android:gravity="top"
            android:hint="message"
            android:textColor="@color/orangedark"

            android:textColorHint="@color/orangetexthint" />

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>